a = (1,2,3,4,5)
print(type(a))
# we cannot alter the tuple elements
print(a)
#tuple methods
print(a.count(1)) #counts occurences of the element.
print(a.index(1)) #returns the index of the element. like in this function we want to know the index of the element 1. It will return its index which is 0.
